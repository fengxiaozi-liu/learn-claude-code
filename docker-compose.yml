services:
  agent:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    stdin_open: true
    tty: true
    command: python agents/s_full.py

  web:
    build:
      context: .
      dockerfile: web/Dockerfile
    ports:
      - "3000:80"
