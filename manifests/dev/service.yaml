apiVersion: v1
kind: Service
metadata:
  name: ai-study-web
  namespace: __DEV_NAMESPACE__
  labels:
    app: ai-study-web
spec:
  selector:
    server-name: ai-study-web
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
